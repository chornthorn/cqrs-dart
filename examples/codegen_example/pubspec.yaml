name: codegen_example
description: Sample app showing pure Dart CQRS with cqrs_codegen auto-registration.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  cqrs:
  cqrs_codegen:

dev_dependencies:
  build_runner: ^2.4.0
  lints: ^6.0.0
  test: ^1.25.6
