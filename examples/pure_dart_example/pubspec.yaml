name: pure_dart_example
description: Sample app showing pure Dart CQRS with manual registration and zero dependencies.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  cqrs:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
