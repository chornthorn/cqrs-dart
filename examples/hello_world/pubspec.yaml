name: hello_world
description: Sample app showing how to write features against cqrs.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  cqrs:
  get_it: ^9.2.1
  injectable: ^3.0.0

dev_dependencies:
  build_runner: ^2.4.0
  injectable_generator: ^3.0.0
  lints: ^6.0.0
  test: ^1.25.6
