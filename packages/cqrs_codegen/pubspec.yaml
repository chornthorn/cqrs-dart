name: cqrs_codegen
description: Code generator for cqrs package to auto-discover and register CQRS handlers.
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  analyzer: ">=6.0.0 <15.0.0"
  build: ">=2.4.0 <5.0.0"
  code_builder: ^4.10.0
  cqrs:
  glob: ^2.1.0
  source_gen: ^4.1.1

dev_dependencies:
  build_runner: ^2.4.0
  lints: ^6.0.0
  test: ^1.25.6
